using System.Collections.Generic;
using UnityEngine;

namespace Programming.Fraction_Engine
{
    /*
    [CreateAssetMenu(fileName = "FractionVisualisationStyle", menuName = "ScriptableObjects/FractionVisualisationStyle",
        order = 2)]
    public class FractionVisualisationStyleInfo : ScriptableObject
    {
        public Material Material;
        public string _figureName;
        public bool isFlipped;

        public void ApplyToObject(GameObject gameObject)
        {
            gameObject.GetComponent<MeshRenderer>().material = Material;
            gameObject.name = _figureName;
            if (isFlipped)
            {
                gameObject.transform.Rotate(Vector3.up, 180.0f);
            }
        }
    }
    */
}